<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.app.continentalapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/icon_game"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/icon_game_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ContinentalApp">
        <activity
            android:name=".screens.resultsActivity.ResultsActivity"
            android:exported="false" />
        <activity
            android:name=".screens.roundsActivity.RoundsActivity"
            android:exported="false" />
        <activity
            android:name=".screens.resumeNames.ResumeNamesActivity"
            android:exported="false" />
        <activity
            android:name=".screens.inputNames.InputNamesActivity"
            android:exported="false" />
        <activity
            android:name=".screens.main.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>